<h1>すべてのレビュー</h1>
<%# <%= link_to 'New Post!', new_post_path %>
<table>
  <% @posts.each do |post| %>
  <tr>
    <th>映画のタイトル</th>
  </tr>
  <tr>
    <td><%= post.title %></td>
  </tr>
  <tr>
    <th align="left">レビュー</th>
  </tr>
  <tr>
    <td><%= post.content %></td>
  </tr>
  <% end %>
</table>